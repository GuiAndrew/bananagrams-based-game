# react-app for WCS course

////////////////////////////////
- To install materialize-css:
$ npm install materialize-css
or 
$ yarn add materialize-css

- To install react-router-dom:
$ npm install react-router-dom
or 
yarn add react-router-dom

////////////////////////////////

every -> method tests whether all elements in the array pass the test implemented by the provided function. It returns a Boolean value.

Higher-Order -> The Higoc-Order Component (HOC), as defined by Dan Abramov (co-author of Redux), is just a function that takes an 
existing component and returns another component, wrapping it (wrapper) assigned to the new features.

